You can set the brightness of an LED with a predefined value and that you can also turn it on/off.
Implement your own timekeeping utilities via Ticker/Timeout etc. and print the time everytime button is pressed. 
Create a data structure that can hold the last 10 presses and initialise it in such a way that the system starts with 0 brightness. 
"button bouncing" problem. Demonstrate that every time button is pressed, IRQ for that specific button is disabled for BUTTON_DEBOUNCE_TIME_MS amount of time.
A function that takes in a pointer for a clockbuffer, and then returns a brightness value between 0.0f and 1.0f. 
Create an array of structs/classes that can hold button-LED pin pairs along with some clockbuffers. 
